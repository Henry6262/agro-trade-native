import React from 'react';
import { View, Text } from 'react-native';

/**
 * Placeholder entry for the Dashboard page hierarchy.
 * Seller/Buyer/Transporter sections will be migrated here.
 */
export function DashboardPagePlaceholder() {
  return (
    <View>
      <Text>Dashboard page migration in progress...</Text>
    </View>
  );
}

export default DashboardPagePlaceholder;
