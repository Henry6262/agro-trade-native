# Public pilot contact shown on the landing page. Leave empty until the inbox
# and its domain are verified and monitored.
NEXT_PUBLIC_PILOT_CONTACT_EMAIL=

# Optional server-side delivery for the dormant /api/waitlist endpoint.
# RESEND_FROM_EMAIL must be a verified sender address.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
